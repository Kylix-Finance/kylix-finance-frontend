import { Button } from "@mui/material";

const Supply = () => {
  return (
    <div className="flex flex-col gap-6">
      <Button
        variant="contained"
        className="!capitalize !text-white !text-xs !font-semibold !leading-4 !text-center !rounded !py-2 !px-3"
        size="large"
      >
        Supply
      </Button>
    </div>
  );
};

export default Supply;
